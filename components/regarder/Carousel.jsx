import React from 'react';

const Carousel = ({ image }) => {
  const { name } = image;
  return (
    <div>
      {}
      <Image src="Fire" alt="" />
    </div>
  );
};

export default Carousel;
